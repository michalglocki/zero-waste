# Required — Cursor API key (https://cursor.com/dashboard → API Keys)
# Also set as a GitHub Actions secret named CURSOR_API_KEY for CI.
CURSOR_API_KEY=

# Optional — Cursor model id (default: composer-2.5)
# CURSOR_MODEL=
